apiVersion: v1
kind: Pod
metadata:
  name: nginx_pod_from_agrocd
  labels:
    run: pod
spec:
  containers:
  - image: nginx
    name: pod
    resources : {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
